<div id="header">
    <div id="logo">
        <h1>
            <a href="{{ path('app_main_index') }}">Hangman</a>
        </h1>
    </div>
    <div id="menu">
        <ul>
            <li class="first current_page_item">
                <a href="{{ path('app_game_index') }}">Game</a>
            </li>
            <li>
                <a href="{{ path('app_main_register') }}">Register</a>
            </li>
            <li>
                {% if app.user %}
                    <a href="{{ path('app_security_logout') }}">Logout</a>
                {% else %}
                    <a href="{{ path('app_main_index') }}">Login</a>
                {% endif %}
            </li>
            <li>
                <a href="{{ path('app_main_contact') }}">Contact</a>
            </li>
        </ul>
        <br class="clearfix" />
    </div>
</div>
